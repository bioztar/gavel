{
  "name": "chair-video-stage",
  "private": true,
  "version": "1.0.0",
  "description": "Browser stage page for the Director WebRTC session — built once, committed as static/stage.bundle.js. No runtime CDN import: see README's Director section for why (alpha package, subpath export resolution, conference wifi).",
  "scripts": {
    "build": "esbuild src/entry.mjs --bundle --format=esm --outfile=../static/stage.bundle.js && node scripts/harden-bundle.mjs"
  },
  "dependencies": {
    "@fal-ai/client": "1.11.0-alpha.3"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
